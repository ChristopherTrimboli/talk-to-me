{
  "name": "talk-to-me",
  "version": "1.0.0",
  "description": "Talk to Me",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-migrate": "flyway -user=root -password=HelloThere69  -schemas=talk-to-me -url=jdbc:mysql://localhost:3306?useSSL=false -locations=filesystem:database/sql migrate"
  },
  "author": "Chris Eddy",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^3.0.6",
    "bluebird": "^3.5.5",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mysql": "^2.17.1"
  }
}
